# rHWP로 HWPX 생성·삽입·검증하는 안내

> 대상: 확정된 CBL 수업안 Markdown과 활동지 PDF·PNG를 받아 HWPX를 제작·검증하는 담당자
>
> 목표: 원문·확정 수업안·활동지 원본을 구분한 상태에서 rHWP로 HWPX를 생성하고, PDF·이미지를 넣고, 열기·역추출·렌더링 검증까지 수행한다.

## 0. 이 문서가 다루는 범위

이 저장소에는 Markdown 기록만 둔다. 생성 중인 `.hwpx`, 활동지 `.html`, `.pdf`, `.png`, JSON 명세, 렌더링 결과물은 **별도 작업 폴더**에만 둔다.

rHWP는 이 컴퓨터에 `rhwp v0.8.6`으로 설치되어 있다. 실제 설치 버전과 명령 옵션은 작업 시작 시 다음으로 확인한다.

```bash
rhwp --version
rhwp scaffold --help
rhwp edit insert-image --help
```

이 안내의 명령은 파일명·경로를 예시로 든 것이다. 양식 버전, 파일 위치, 삽입 좌표는 매 작업에서 실제 값으로 바꾼다. `HWPX ZIP/XML`을 압축 해제해 문자열 치환으로 고치지 않는다.

## 1. 역할과 인수 기준

| 역할 | 받는 것 | 하는 일 | 남기는 기록 |
|---|---|---|---|
| 콘텐츠 작성자 | 원문, 피드백 | 교사별 1개 주제의 TRACES 6차시 Markdown 확정 | 확정 수업안 Markdown |
| 활동지 제작자 | 확정 수업안 | 학생용·교사용 예시 HTML, PDF 또는 PNG 생성 | 별도 작업 폴더의 원본과 파일 정보 |
| HWPX 생성 담당자 | 확정 수업안, 생성 요청서, 승인 양식, 삽입 파일 | HWPX 생성·이미지 삽입·파일 정보 전달 | `HWPX-생성-요청서.md`의 기록 |
| 검증 담당자 | 생성된 HWPX | 열기, ZIP, 역추출, 렌더링, 내용 대조 | `HWPX-검증-보고서.md` |

생성을 시작하려면 다음이 모두 갖춰져야 한다.

- 수업안 Markdown의 문서 상태가 `확정`이다.
- 빈 대괄호, `교사 확인 필요`, 미확정 재구성 제안이 없다.
- 교사, 학년, 과목, 단원, 성취기준, 총 차시, 평가 기준이 확정돼 있다.
- 6차시 TRACES 계획과 차시별 산출물이 서로 맞는다.
- 활동지 파일은 학생용/교사용 예시 구분, 버전, 삽입 위치가 적혀 있다.
- HWPX 생성 요청서는 [`templates/HWPX-생성-요청서.md`](templates/HWPX-생성-요청서.md) 형식으로 작성돼 있다.

하나라도 빠지면 HWPX를 “임시 완성본”으로 만들지 말고 Markdown 단계로 되돌린다.

## 2. 별도 작업 폴더와 파일명

예시 작업 폴더는 아래처럼 원문, 명세, 중간 파일, 최종본, 검증 산출물을 분리한다.

```text
/별도작업/2026-김교사-함수와그래프-주제/
  input/
    김교사_함수와그래프_주제_TRACES6차시.md
    HWPX-생성-요청서.md
    학생용-3차시-활동지.pdf
    교사용-3차시-예시.png
  spec/
    lesson-spec.json
  output/
    김교사_함수와그래프_주제_TRACES6차시_v01.hwpx
  render/
    pdf/
    png/
    markdown/
  verification/
    sha256.txt
    검증메모.md
```

- 파일명은 `교사명_과목_주제_TRACES6차시_v01.hwpx`처럼 내용·버전이 보이게 정한다.
- 재생성하면 `v02`처럼 버전을 올리고, 이전 파일을 덮어쓰지 않는다.
- 원본 HTML, PDF, PNG와 HWPX는 이 Git 저장소에 커밋하지 않는다.
- 이 저장소에는 최종 확정 Markdown, 생성 요청서, 검증 보고서만 반영한다.

## 3. 생성 방식 선택

### 방식 A: 승인된 HWPX 양식에 내용을 채우는 방식

학교 또는 동아리에서 이미 확정한 HWPX 양식이 있으면 이 방식을 우선한다.

1. 양식 파일명·버전·소유자를 생성 요청서에 기록한다.
2. 양식에 누름틀(필드)이 있으면 먼저 목록을 확인한다.
3. 승인된 생성 도구 또는 rHWP의 편집 기능으로 제목, 표, 문단, 필드를 채운다.
4. 표 열 순서·병합·수식 영역·공통 서식을 임의로 바꾸지 않는다.
5. 활동지 PDF·PNG는 지정된 부록 또는 차시 자료 위치에 삽입한다.

양식에 어떤 필드가 있는지 볼 때는 다음처럼 확인한다.

```bash
rhwp fields "/별도작업/양식/수업지도안_양식.hwpx"
rhwp info "/별도작업/양식/수업지도안_양식.hwpx"
```

필드 채우기, 표 셀 입력, 문단 삽입 같은 편집 기능은 실제 양식 복사본에서 한 항목씩 시험하고 결과를 렌더링해 본다. 원본 양식은 수정하지 않는다.

### 방식 B: 구조화된 JSON 명세로 새 HWPX를 생성하는 방식

승인된 양식이 없고 새 기본 문서를 만들 때만 rHWP `scaffold`를 쓴다. `scaffold`는 제목·개요 제목·문단·표가 포함된 JSON 명세에서 유효한 HWPX를 생성한다.

1. 확정 Markdown의 제목, 기본 정보, 6차시 표, 예상 산출물 표를 JSON 명세로 옮긴다.
2. 명세에는 확정된 값만 넣는다. 미확정 제안이나 원문 근거 없는 문장을 넣지 않는다.
3. JSON 문법을 확인한다.
4. `rhwp scaffold`로 작업 폴더의 HWPX를 생성한다.
5. 생성 직후 열기·역추출·PDF 렌더링을 수행한다.

명령 형태는 다음과 같다.

```bash
rhwp scaffold "/별도작업/spec/lesson-spec.json" \
  --format hwpx \
  -o "/별도작업/output/김교사_함수와그래프_주제_TRACES6차시_v01.hwpx"
```

명세 구조와 지원 요소는 설치된 버전에서 `rhwp scaffold --help`와 `rhwp capabilities`로 먼저 확인한다. 지원하지 않는 복잡한 배치·수식·도형을 JSON으로 억지로 구현하지 말고, 승인된 양식 방식이나 적절한 생성 도구로 전환한다.

## 4. 활동지 PDF·PNG를 HWPX에 넣는 방법

### 4-1. 무엇을 넣을지 먼저 결정한다

- 인쇄 품질·여러 쪽 보존이 중요하면 PDF를 원본으로 보관하고, HWPX에 넣는 방식은 승인 양식·도구의 지원 여부를 확인한다.
- HWPX에 그림으로 넣어야 한다면 PDF의 해당 쪽을 충분한 해상도의 PNG로 만들어 넣는다.
- 학생용 활동지와 교사용 예시를 파일명과 HWPX 제목에서 분명히 구분한다.
- 이미지에는 학생의 개인정보나 무단 사용 이미지가 없어야 한다.

rHWP `edit insert-image`는 PNG·JPG·JPEG·BMP·TIF·TIFF 삽입을 지원한다. PDF를 바로 넣는 기능으로 가정하지 않는다. PDF를 써야 하는 양식이라면 승인된 생성 도구에서 실제 삽입 여부를 먼저 시험한다.

### 4-2. rHWP 이미지 삽입 명령

먼저 원본 HWPX를 복사본으로 만든다. 그리고 실제 삽입 전에 `--dry-run`으로 페이지·좌표·크기를 확인한다.

```bash
# 삽입 예정만 확인한다. 페이지 번호는 0부터 시작한다.
rhwp edit insert-image \
  "/별도작업/output/김교사_함수와그래프_주제_TRACES6차시_v01.hwpx" \
  --image "/별도작업/input/교사용-3차시-예시.png" \
  --page 6 --x 7200 --y 10800 --width 45000 \
  --dry-run

# 문제가 없으면 새 버전 파일로 쓴다.
rhwp edit insert-image \
  "/별도작업/output/김교사_함수와그래프_주제_TRACES6차시_v01.hwpx" \
  --image "/별도작업/input/교사용-3차시-예시.png" \
  --page 6 --x 7200 --y 10800 --width 45000 \
  -o "/별도작업/output/김교사_함수와그래프_주제_TRACES6차시_v02.hwpx"
```

주의 사항:

- `--page`는 0부터 시작한다. `--page 6`은 일곱 번째 쪽이다.
- 좌표와 크기는 픽셀이 아니라 **HWPUNIT**이다. `1 inch = 7200 HWPUNIT`, A4 세로 크기는 약 `59528 x 84188 HWPUNIT`다.
- `--width` 또는 `--height` 중 하나만 주면 비율을 유지한다. 둘 다 주면 이미지가 찌그러질 수 있다.
- `--json` 출력을 쓰면 쪽 밖으로 나가는 `overflow` 신호를 확인할 수 있다. overflow가 있으면 좌표·크기·쪽 구성을 고친다.
- 좌표 삽입은 특히 양식의 표·머리말·본문과 겹칠 수 있다. 반드시 PDF/PNG 렌더링으로 사람이 확인한다.

## 5. 생성 뒤 반드시 하는 검증

“파일이 생성됐다”는 것은 검증이 아니다. 최소한 아래 순서대로 한다.

### 5-1. 파일 형식과 열기 확인

```bash
FILE="/별도작업/output/김교사_함수와그래프_주제_TRACES6차시_v02.hwpx"

rhwp info "$FILE"
rhwp verify "$FILE" --expect-format hwpx --expect-min-pages 1
sha256sum "$FILE" > /별도작업/verification/sha256.txt
```

`rhwp verify`가 통과해도 사람의 표·수식·이미지 검수를 대신하지 않는다. 이는 형식·페이지·문자열 등 기계적으로 확인 가능한 조건을 검사하는 단계다.

### 5-2. Markdown으로 역추출해 내용 대조

```bash
mkdir -p /별도작업/render/markdown
rhwp export-markdown "$FILE" -o /별도작업/render/markdown
```

역추출본과 확정 Markdown을 대조한다.

- 제목, 교사, 학년, 과목, 단원, 성취기준
- 총 차시와 1~6차시 활동·산출물·평가 증거
- 안전·개인정보·AI 경계
- 학생용/교사용 예시 제목과 삽입 위치
- 원문 근거 없는 문장이나 미확정 표시의 유입 여부

텍스트 역추출은 이미지의 위치·선명도·표의 잘림을 충분히 알려 주지 않는다. 그래서 다음 렌더링 검사가 필요하다.

### 5-3. PDF·PNG 렌더링으로 실제 조판 확인

```bash
mkdir -p /별도작업/render/pdf /별도작업/render/png
rhwp export-pdf "$FILE" \
  -o /별도작업/render/pdf/최종검수용.pdf \
  --profile high-quality
rhwp export-png "$FILE" \
  -o /별도작업/render/png \
  --profile high-quality --dpi 180
```

렌더링 결과를 열어 다음을 사람이 확인한다.

- 표가 쪽 밖으로 나가거나 중간에서 잘리지 않는가?
- 수식, 그래프, 이미지, 활동지 쓰기 공간이 겹치지 않는가?
- 글자가 너무 작거나 폰트가 깨지지 않았는가?
- 학생용과 교사용 예시가 뒤섞이지 않았는가?
- 활동지 이미지의 문장이 읽히고, 의도한 쪽에 들어갔는가?
- 머리말·꼬리말·쪽 번호가 양식 규정과 맞는가?

필요하면 `rhwp layout-anomaly`와 `rhwp render-diff`를 보조 검토에 쓸 수 있지만, 사람의 실제 페이지 열람을 생략하지 않는다.

### 5-4. 이 저장소에 남길 검증 기록

생성 파일을 Git에 넣지 않는다. 대신 [`templates/HWPX-검증-보고서.md`](templates/HWPX-검증-보고서.md)를 복사해 다음을 적는다.

- 생성 요청 ID와 확정 수업안 Markdown 경로
- HWPX 파일명, 버전, 생성 도구·버전, 양식 식별자·버전
- SHA-256, 생성·검증 일시
- ZIP/열기, Markdown 역추출, PDF·PNG 렌더링, 표·수식·이미지·쪽 구성 대조 결과
- 차이와 재생성 조치
- 배포 가능 또는 배포 보류 판정

## 6. 실패했을 때의 처리

| 문제 | 먼저 할 일 | 처리 |
|---|---|---|
| HWPX가 열리지 않음 | 원본 양식·생성 명세·도구 버전 확인 | 배포 보류, 새 버전으로 재생성 |
| 표·수식·이미지가 잘림 | PDF·PNG 렌더링으로 정확한 쪽과 위치 확인 | 글자 축소 대신 표 구조·이미지 크기·쪽 나눔을 수정 |
| 활동지 이미지가 흐림 | 원본 PDF와 PNG 픽셀 크기 확인 | 더 높은 해상도의 쪽별 PNG를 만들고 재삽입 |
| 역추출 내용이 확정 Markdown과 다름 | 누락·추가·차시 수를 목록화 | 생성 입력을 수정하고 새 버전으로 재생성 |
| 미확정 제안이 들어감 | 확정 Markdown과 생성 요청서 상태 확인 | HWPX 배포 보류 후 Markdown 단계에서 교사 확인 |
| 원문 양식이 손상됨 | 원본을 더 이상 편집하지 않음 | 원본 양식의 새 복사본으로 다시 시작 |

## 최종 점검표

- [ ] 확정 Markdown과 생성 요청서가 모두 준비돼 있다.
- [ ] 생성 파일은 별도 작업 폴더에만 있고 이 저장소에 넣지 않았다.
- [ ] HWPX 파일명·버전·도구·양식·SHA-256을 기록했다.
- [ ] HWPX 열기와 형식 확인을 통과했다.
- [ ] Markdown 역추출로 제목·6차시·산출물·안전 경계를 대조했다.
- [ ] PDF 또는 PNG 렌더링을 실제로 열어 표·수식·이미지·쪽 구성을 확인했다.
- [ ] 이미지 삽입 위치와 학생용/교사용 구분을 확인했다.
- [ ] 검증 보고서에 통과·차이·재생성 내역과 최종 판정을 기록했다.
